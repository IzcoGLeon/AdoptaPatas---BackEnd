- This is the repository for the BackEnd side of the project 
"AdoptaPatas", as for now it is being used to implement a local server 
using express and node.js so that CRUD funcionalities can be tested
